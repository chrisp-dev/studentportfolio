<div class="w-full flex justify-between mx-auto text-white bg-black">
    <div class="navbar-brand flex justify-center p-2">Chris Paradeise</div>
    <ul class="flex justify-center">
        <a class="p-2 hover:bg-gray-500 border-r-2 {{#if active.about}}active{{/if}}" href="/">About</a>
        <a class="p-2 hover:bg-gray-500 border-r-2 {{#if active.portfolio}}active{{/if}}"
            href="/portfolio">Portfolio</a>
        <a class="p-2 hover:bg-gray-500 {{#if active.contact}}active{{/if}}" href="/contact">Contact</a>
    </ul>
</div>